# Data

The dataset powering this dashboard is embedded within the Power BI file (`dashboard/analytics_dashboard.pbix`).
No external data files are required — open the `.pbix` file directly in Power BI Desktop.

To connect to a live data source, update the data source connection settings within Power BI Desktop
under Home → Transform Data → Data Source Settings.
